// src/app/layout.tsx
import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-sans",
});

export const metadata: Metadata = {
  title: "CoverCapture | Insurance Workstream Automation",
  description:
    "Modernizing commercial insurance through data-driven automation.",
  icons: {
    icon: "/brand/favicon-16x16.png",
    shortcut: "/brand/favicon-16x16.png",
    apple: "/brand/favicon-16x16.png",
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}
